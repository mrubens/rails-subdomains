<h1>Admin Dashboard</h1>

<p>You're on the <strong><%= @subdomain %></strong> subdomain.</p>

<p>This page is only accessible via the "admin" subdomain.</p>

<p><%= link_to "← Back to main site", root_url(subdomain: false) %></p>
